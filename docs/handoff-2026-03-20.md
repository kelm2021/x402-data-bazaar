# x402 Data Bazaar - Session Handoff (2026-03-20)

## Production Snapshot

- Main app: https://x402-data-bazaar.vercel.app
- Standalone #1: https://restricted-party-screen.vercel.app
- Standalone #2: https://vendor-entity-brief.vercel.app

## Completed This Session

1. Mapped route categories to recognized 402index taxonomy in main app.
2. Fixed canonical metadata for these two URLs so they self-describe correctly:
   - /api/exchange-rates/USD
   - /api/weather/current
3. Added dedicated /api discovery feeds to both standalone apps with routeKey, priceUsd, category, exampleUrl.
4. Standardized standalone categories to recognized taxonomy (`identity`).
5. Removed query strings from standalone canonical URLs to avoid metadata mismatch.
6. Fixed standalone runtime packaging issue by vendoring payment-required-compat helper into each app.
7. Addressed clawbot matching issues in standalone apps:
   - facilitator default reverted to standard CDP URL (`https://api.cdp.coinbase.com/platform/v2/x402`)
   - added payment payload normalization (strip non-core accepted fields before verify)
   - disabled synthetic fast unpaid 402 path by default (middleware-consistent challenge path)
8. Decommissioned FEC endpoint from main app:
   - removed route metadata and mounting
   - removed portfolio references
   - removed routes/fec.js

## Validation Performed

- Root tests: `npm test` -> pass (118/118)
- Standalone tests:
  - `npm --prefix apps/restricted-party-screen test` -> pass
  - `npm --prefix apps/vendor-entity-brief test` -> pass
- Live checks:
  - `GET /api/fec/candidates` now returns 404 on main app
  - Main `/api` now reports 19 endpoints and no FEC route
  - Standalone 402 headers now show standard facilitator URL (`/platform/v2/x402`)
  - Standalone 402 headers include category `identity` and canonical resource URLs

## Important Current Reality

- 402index API can lag behind live headers/catalog changes.
- Some entries may still appear as `uncategorized` or stale for 1-2 crawler cycles.
- `vendor-entity-brief` may still not appear in 402index until discovery refresh + at least one settled payment signal.

## Recommended First Steps Next Session

1. Re-run clawbot paid calls against both standalone endpoints.
2. If either still fails with "No matching payment requirements", capture and share:
   - raw `PAYMENT-REQUIRED` challenge
   - paid retry request headers/body summary
   - paid retry response headers/body summary
3. Re-check 402index after 1-2 hourly cycles for:
   - exchange-rates/USD
   - weather/current
   - restricted-party-screen
   - vendor-entity-brief

## High-Signal Commands

- Check main discovery quickly:
  - `curl https://x402-data-bazaar.vercel.app/api`
- Check standalone discovery feeds:
  - `curl https://restricted-party-screen.vercel.app/api`
  - `curl https://vendor-entity-brief.vercel.app/api`
- Check 402index entries:
  - `curl "https://402index.io/api/v1/services?q=x402-data-bazaar.vercel.app&limit=200"`
  - `curl "https://402index.io/api/v1/services?q=restricted-party-screen.vercel.app&limit=50"`
  - `curl "https://402index.io/api/v1/services?q=vendor-entity-brief.vercel.app&limit=50"`

## Files Touched in This Session (high value)

- app.js
- test/payment.test.js
- portfolio/blueprints.js
- portfolio/strategy.js
- portfolio/seller-manifest.json
- routes/fec.js (deleted)
- apps/restricted-party-screen/app.js
- apps/restricted-party-screen/seller.config.json
- apps/restricted-party-screen/lib/payment-required-compat.js
- apps/restricted-party-screen/test/seller.test.js
- apps/vendor-entity-brief/app.js
- apps/vendor-entity-brief/seller.config.json
- apps/vendor-entity-brief/lib/payment-required-compat.js
- apps/vendor-entity-brief/test/seller.test.js
